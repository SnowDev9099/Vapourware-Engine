// game.js
const game = new Phaser.Game('100%', '100%', Phaser.AUTO, 'gameCanvas', {
    preload: () => {
    
    },
    create: () => {
        
    },
    update: (time, delta) => {
   
    }
});

// Add event listeners for user input
window.addEventListener('keydown', (event) => {
    
});
